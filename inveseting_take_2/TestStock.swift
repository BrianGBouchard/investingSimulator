import Foundation

// Creates a struct that mimics the "stock" entity (as defined in the data model) to test the validity of a web request

struct TestStock {
    var symbol: String
    var name: String
    var price: Double
}
